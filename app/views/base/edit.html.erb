<%= simple_form_for resource, url: resource.persisted? ? resource_path : collection_path do |f| %>
  <%= render 'form_errors', f: f if f.object.errors[:base].any? %>
  <%= render 'form', f: f %>
  <%= render 'form_buttons', f: f %>
<% end %>
